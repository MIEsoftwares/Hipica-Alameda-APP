import { SafeAreaView, Text } from "react-native";


export default function Schedule({ navigation }){
    return (
        <SafeAreaView>
            <Text children="a"/>
        </SafeAreaView>
    );
}